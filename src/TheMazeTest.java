// -------------------------------------------------------------------------
/**
 * Test TheMaze Class
 *
 * @author Michael Goheen
 * @version Apr 10, 2012
 */
public class TheMazeTest
{
    // ----------------------------------------------------------
    /**
     * Main method to test
     *
     * @param args
     */
    public static void main(String args[])
    {
        //int testInt;
        TheMaze m = new TheMaze(0, 1);
        //Edge testRemove;
        /*
        testInt = m.getWalls();
        System.out.println(": " + testInt);
        testInt = m.getWalls();
        System.out.println("Total Walls: " + testInt);
        testInt = m.getInternalWallsRemaining();
        System.out.println("Internal Walls: " + testInt);
        testInt = m.getRooms();
        System.out.println("Rooms: " + testInt);
        Edge internalWalls[];
        internalWalls = m.getInternalWalls();
        System.out.print("InternalWalls: ");
        for (int i = 0; i < internalWalls.length; i++)
        {
            System.out.print(internalWalls[i] + " \n");
        }
        m.removeWall();
        m.removeWall();
        m.removeWall();
        m.removeWall();
        internalWalls = m.getInternalWalls();
        System.out.print("InternalWalls: ");
        for (int i = 0; i < internalWalls.length; i++)
        {
            System.out.print(internalWalls[i] + " \n");
        }*/
        System.out.println(m);

    }
}
